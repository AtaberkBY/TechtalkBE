server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    username: sa
    password: 12345678
    url: jdbc:sqlserver://DESKTOP-LL3BIEJ;databaseName=Project
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServer
        format_sql: true
    show_sql: true
